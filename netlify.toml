[build]
  command = "pnpm build"
  publish = "dist"
  functions = "netlify/functions"

[dev]
  command = "pnpm dev"
  targetPort = 5173
  port = 8888
  autoLaunch = false

# SPA redirect - enables client-side routing in production
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

